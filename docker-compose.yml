services:
  frontend:
    build: .
    container_name: ticket-classifier-web
    ports:
      - "3000:3000"
    environment:
      - NEXT_PUBLIC_API_URL=http://localhost:8000/api
    volumes:
      - .:/app
      - /app/node_modules
    command: npm run dev